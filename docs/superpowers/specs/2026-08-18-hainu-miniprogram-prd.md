# 海大工坊 · 微信小程序 — 产品需求文档 (PRD)

> 版本：v1.14
> 日期：2026-08-19
> 状态：初稿

---

## 1. 产品概述

### 1.1 产品定位

海南大学校园工具微信小程序，面向全体在校本科生、研究生及入学前新生，提供校园信息查询、生活服务、社区交流等实用功能。**不接入校园内部账号认证体系**，所有数据由运营团队人工维护。

### 1.2 目标用户

| 用户身份 | 说明 |
|:--------|:-----|
| 新生（本+研） | 入学前使用，可访问新生模块 + 智慧海大模块 |
| 本科生 | 在校本科生，可访问社区模块 + 课表模块 + 智慧海大模块 |
| 研究生 | 在校研究生，可访问社区模块 + 课表模块 + 智慧海大模块 |

### 1.3 核心目标

- 为新生提供入学前后的全套信息指引
- 为在校生提供课表、校园信息查询、社区交流等日常工具
- 无需任何校内接口，纯人工维护即可运行

---

## 2. 用户与身份系统

### 2.1 用户注册

- 用户新增**仅允许在小程序端**完成
- **注册流程**（新用户初始访问）：
  1. 新用户首次进入小程序，先选择身份（新生 / 本科生 / 研究生）
  2. 程序记忆身份选择
  3. 进入微信授权登录
  4. 授权成功后，系统自动注入之前选择的身份进行保存
  5. 授权登录即视为注册成功，自动创建用户并同步到管理后台「用户管理」
- 注册成功后用户即可使用 **UID + 默认密码** 在网页端登录
- 未授权登录前，用户以「游客」身份访问（见 §2.8 游客模式）

### 2.2 用户信息维度

| 维度 | 说明 |
|:----|:-----|
| 昵称 | 注册后明确 |
| 头像 | 注册后明确 |
| 邮箱 | 可自行补全 |
| QQ | 可自行补全 |
| 微信号 | 可自行补全 |
| UID | 注册时自动生成的身份码 |
| 积分 | 默认关闭（积分状态），认证通过后开启 |
| 身份 | 新生 / 本科生 / 研究生 |
| 认证状态 | 默认未认证 |

### 2.3 身份选择

- 新用户注册后进入身份选择页，选择身份：新生 / 本科生 / 研究生
- 身份选择后长期保存，后续功能按身份判断权限与数据隔离
- 每人最多修改 3 次身份（含首次选择），在个人设置中修改
- **冷却期**：修改身份后 24 小时内不可再次修改
- **身份修改后数据迁移规则**：
  - 课表数据：原身份课表保留但不再展示，新身份课表为空（需重新 DIY/导入/复刻）
  - 课表分享码：原身份分享码自动停用
  - 二手集市商品：保留（在校生通用数据，不受身份切换影响）
  - 找室友信息：保留（如仍为新生身份适用；若改为本/研则隐藏入口）

### 2.4 UID 与密码

- **UID**：注册时自动生成的身份码，从 `随机6位` 开始递增，全局唯一
- **默认密码**：注册时随机生成；用户可在小程序「个人信息」页查看默认密码
- **密码修改**：用户可在小程序个人信息页或网页端修改密码
- **登录方式**：网页版与小程序的用户均可通过「UID + 密码」登录
- **微信与 UID 绑定**：一个微信对应一个 UID，换微信号视为新用户注册新 UID；原 UID 可用密码登录网页端
- **登录态保持**：小程序授权登录后长期保持登录态，下次打开无需再次点击登录；网页端登录态按会话保持

### 2.5 初始信息

新用户通过小程序注册身份后，明确的信息仅有：昵称、头像、UID、身份。
其余维度（邮箱、QQ、微信号等）可在小程序「个人信息」或网页端自行补全。

### 2.6 积分与认证

- **积分**：默认关闭（积分状态），积分值为 0
- **认证**：默认「未认证」状态
- **功能限制**：进入二手集市、浏览商品信息无认证要求；**发布商品信息必须为「已认证」状态**
- **认证流程**：
  1. 用户点击「申请认证」
  2. 填写：姓名、学号、专业
  3. 上传证明身份的图片（一卡通照片或数字认证截图）
  4. 提交审核
- **人工审核**：管理后台统一进行人工审核
- **审核通过后**：认证状态转为「已认证」，需要认证才能使用的权限全部开通，同时该用户积分状态开启
- **积分获取规则**（积分状态开启后生效）：
  - 每日签到：+1 分（每日限 1 次，防刷机制：一天仅能签到一次，按自然日计算）
  - 发布二手商品：+2 分（每条）
  - 认证通过：+10 分（一次性）
- **积分用途**：当前阶段仅记录与展示，预留后续兑换功能
- **积分管理**：管理后台可手动调整用户积分
- **认证驳回与重试**：
  - 驳回时管理员填写驳回原因，用户在认证中心可查看驳回原因
  - 用户可修改信息后重新申请，**限 3 次**（含首次申请）
  - 超过 3 次后不可再申请，需联系管理员人工处理

### 2.7 隐私设置

- 用户可在小程序「个人信息-隐私设置」中修改隐私状态
- 隐私状态开启后：其他用户点击该用户主页时，仅能查看到该用户设置的**对外展示信息**
- 对外展示信息由用户自行切换各信息维度的开启/关闭（如昵称、头像、邮箱、QQ、微信号等）

### 2.8 游客模式

未授权登录的用户以「游客」身份访问小程序：

- **可访问**：新生模块、智慧海大模块的内容浏览（需游客身份有效），课表模块的**框架与样式预览**（仅显示空课表结构，无课程内容）
- **不可访问**：社区模块全部功能（二手集市等）
- **登录拦截**：游客点击社区模块功能时，提示「请先登录」并附「去登录」快捷按钮
- **课表限制**：游客点击课表仅能看到课表框架与样式（周视图网格、今日视图布局），不展示任何课程数据；登录后可查看完整课表
- **网页端游客**：网页端未登录同样适用以上游客规则，与小程序一致

### 2.9 数据隔离规则

| 类型 | 数据范围 | 举例 |
|:----|:---------|:-----|
| 在校生通用 | 本/研身份数据互通，展示相同内容 | 二手集市、电话簿、校历等 |
| 身份隔离 | 本/研身份数据相互不可见 | 课表数据、课表分享码完全隔离 |

### 2.10 通知中心

- 用户在小程序「个人设置」中可查看系统通知
- 通知来源：管理后台「通知管理」发布的通知，按推送对象（全部/本科生/研究生/新生）过滤展示
- 通知列表按时间倒序排列，支持已读/未读标记
- 点击通知进入详情页（标题 + 内容 + 发布时间）
- 通知未读数量在个人设置入口处角标提示
- 通知推送仅限小程序内部信息栏，不推送微信订阅消息/模板消息

### 2.11 用户反馈

- 用户在小程序「个人设置-意见反馈」中提交反馈
- 反馈字段：反馈内容（必填，文本）、联系方式（选填）
- 提交后进入后台「系统监控-用户反馈」待处理列表
- 用户可查看自己的反馈历史与处理状态（待处理/已处理）



## 3. 模块功能定义

### 3.1 模块总览

| 模块 | 新生 | 本科生 | 研究生 |
|:----|:----:|:-----:|:-----:|
| 新生模块 | ✅ | ❌ | ❌ |
| 智慧海大模块 | ✅ | ✅ | ✅ |
| 社区模块 | ❌ | ✅ | ✅ |
| 课表模块 | ❌ | ✅ | ✅ |
| 工具箱 | ❌ | ✅ | ✅ |

### 3.2 新生模块

#### 3.2.1 入学指南

**前端展示形式：**

- 列表页：卡片式条目展示，每张卡片含图标、标题、一句话简介，点击进入详情
- 详情页：富文本图文混排（标题、段落、图片、列表），支持图片点击放大预览

**后台配置内容：**

- 6 个条目（报到流程/军训安排/宿舍分配/校园卡/缴费指南/档案转递）独立编辑
- 每个条目支持：富文本编辑器、图片上传、排序、启停（停用后前端隐藏，数据保留）

#### 3.2.2 生活攻略

**前端展示形式：**

- 校区选择页：4 张校区卡片（海甸/儋州/城西/观澜湖），卡片含校区名称与简介
- 主题列表页：7 宫格图标导航（食堂/快递/超市/就医/运动/打印/交通）
- 内容页：富文本图文混排

**后台配置内容：**

- 按「校区 → 主题」两级树管理，共 4 校区 × 7 主题 = 28 个内容位
- 每个主题支持：富文本编辑器、图片上传、排序、启停
- 主题列表在后台按校区分组展示，支持批量启停

#### 3.2.3 新生 FAQ

**前端展示形式：**

- 顶部搜索框，支持关键词实时过滤
- 分类横向 Tab 导航（可左右滑动）
- 问题列表采用手风琴展开：点击问题展开答案，再点收起，不跳转页面

**后台配置内容：**

- 分类管理：分类名称增删改、排序、启停
- 问题管理：问题、答案、所属分类、排序、启停
- 支持按分类查看问题列表

#### 3.2.4 新生缴费

**前端展示形式：**

- 单页展示：缴费说明文案 + 醒目跳转按钮（图标 + 「前往缴费」按钮）
- 点击按钮跳转缴费小程序

**后台配置内容：**

- 缴费小程序路径维护（归入「站点设置-前端配置」）
- 「站点设置-前端配置」完整配置项：缴费小程序路径、客服联系方式（微信/QQ/电话）、主题色、启动页/导航页背景图、关于我们文案、小程序版本号
- 缴费说明文案编辑

#### 3.2.5 找室友

为新生提供找室友匹配功能，后台可配置功能开启和关闭时间，仅在配置的时间段内对新生开放。

**前端展示形式：**

- 列表页：卡片流展示，卡片含姓名、校区、学院、寝室号、联系方式，支持下拉刷新
- 发布页：表单 + 多级联动选择器（校区→书院→楼栋号、校区→学院→专业）
- 匹配结果页：提交后自动匹配，展示三种结果状态（见下方「自动匹配流程」）
- 我的信息页：展示自己已发布的信息，支持修改（显示剩余修改次数）
- 详情页：完整展示全部字段
- 仅开放时间区间内展示入口，区间外隐藏并提示「功能暂未开放」

**后台配置内容：**

- 功能开放时间配置（开始日期 ~ 结束日期，可整体启停）
- 信息修改次数配置（默认 3 次，可调整）
- 发布信息列表管理：查看、下架、删除
- 智慧校园数据维护（校区/书院/学院/专业/楼栋号）

**发布字段：**

| 字段 | 必填/选填 | 类型 | 说明 |
|:----|:---------|:-----|:------|
| 姓名 | 必填 | 文本输入 | — |
| QQ/微信号 | 必填 | 文本输入 | 联系方式 |
| 校区 | 必选 | 下拉选择 | 数据来源智慧校园模块 |
| 书院 | 必选 | 下拉选择 | 数据来源智慧校园模块 |
| 学院 | 必选 | 下拉选择 | 数据来源智慧校园模块 |
| 专业 | 必选 | 下拉选择 | 数据来源智慧校园模块 |
| 楼栋号 | 必选 | 下拉选择 | 数据来源智慧校园模块 |
| 寝室号 | 必填 | 文本输入 | — |

**自动匹配流程：**

用户在发布页填写并提交表单后，系统自动匹配已有数据（不含自己刚发布的这条）：

`
用户提交表单
    │
    ▼
自动匹配已有数据
    │
    ├── ① 匹配到室友（同校区 + 同书院 + 同楼栋 + 同寝室号）
    │      → 返回「室友匹配结果」+ 室友快捷卡片
    │
    ├── ② 未匹配到室友，但找到同专业（同学院 + 同专业）
    │      → 提示「暂无室友匹配，已找到同专业同学」
    │         + 同专业名单与卡片
    │
    └── ③ 室友与同专业均未匹配到
           → 展示「暂无匹配」结果 + 「进入列表页」快捷入口
`

**发布与修改规则：**

- 无论匹配结果如何，提交即发布，信息出现在找室友列表中
- 每个用户在找室友功能中**仅能发布 1 条信息**
- 已发布信息支持修改，修改次数由后台配置，**默认 3 次**，用尽后不可再修改
- 信息发布后用户**不可删除**，只能修改；删除权限仅限管理后台
- 校区、书院、学院、专业、楼栋号为多级联动下拉选择。选择校区后筛选书院、学院、楼栋号；选择学院后筛选专业
- 找室友卡片展示发布时填写的内容，不受用户隐私设置控制（发布即视为公开联系方式）
- 所有数据由后台智慧校园模块统一维护

### 3.3 智慧海大模块

#### 3.3.1 海大介绍

**前端展示形式：**

- 条目列表：5 张图标卡片（学校概况/校区介绍/校园风光/院系设置/校歌校徽）
- 详情页：概览式富文本 + 图片展示，点到即止不展开长篇
- 校园风光条目：图片瀑布流展示

**后台配置内容：**

- 5 个条目独立编辑：富文本、图片上传、排序、启停
- 校园风光条目支持多图管理（图片列表增删改）
- 院系设置条目：自动读取学院管理+专业管理的数据生成列表，每个学院可附加富文本介绍

#### 3.3.2 电话簿

**前端展示形式：**

- 顶部搜索框，支持部门名称关键词搜索
- 分类横向 Tab（海甸/儋州/城西/观澜湖）
- 电话列表：卡片式（部门名称 + 电话号码 + 拨号按钮），点击拨号按钮直接调起系统电话

**后台配置内容：**

- 电话分类联动「校园数据库-校区管理」自动生成（4 校区）
- 电话条目 CRUD：部门名称、电话号码、备注、所属分类、排序、启停

#### 3.3.3 校历

**前端展示形式：**

- 图片模式：整图展示，支持双指缩放与拖动查看
- 日历视图模式：月历视图，日期上标注颜色圆点与文字标签（学期起止/节假日/考试周），点击标注查看详情

**后台配置内容：**

- 模式切换开关（图片模式 / 日历视图模式）
- 图片模式：校历图片上传
- 日历视图模式：标注管理（标注名称、日期、类型、颜色），支持批量导入与手动增删改

#### 3.3.4 校园地图

**前端展示形式：**

- 底部校区切换 Tab（海甸/儋州/城西/观澜湖）
- 地图大图支持双指缩放与拖动
- 建筑标注点：点击弹出气泡，显示建筑名称 + 简介

**后台配置内容：**

- 按校区管理地图：地图图片上传
- 标注点管理：建筑名称、简介、坐标定位（在图上点击放置/拖动）、排序、启停

#### 3.3.5 校园出行

**前端展示形式：**

- 顶部子功能 Tab（时刻表/车站信息/乘车指南）
- 时刻表：按线路分组的表格或时间线列表
- 车站信息：列表卡片（站名 + 位置描述 + 经过线路）
- 乘车指南：富文本图文

**后台配置内容：**

- 时刻表 CRUD：线路名称、发车时间、发车地点、目的地、备注、排序、启停
- 车站信息 CRUD：站名、位置描述、经过线路、排序、启停
- 乘车指南富文本编辑

### 3.4 社区模块

#### 3.4.1 二手集市

发布字段：

| 字段 | 必填/选填 | 说明 |
|:----|:---------|:------|
| 商品标题 | 必填 | — |
| 商品图片 | 必填 | 最多6张，第1张为主图 |
| 商品描述 | 选填 | 文字描述 |
| 价格 | 必填 | — |
| 联系方式 | 必填 | 微信或QQ号 |
| 分类 | 必选 | 生活用品/食品饮料/电子数码/图书教材/其他 |

规则：
- 纯信息发布，小程序内不处理交易
- **发布商品必须为「已认证」用户**，浏览无认证要求
- 商品上架有效期5天
- 3天后自动下架（保留数据，不删除）
- 用户可手动下架或删除商品
- 重新上架需手动操作
- 商品分类由后台「二手集市-分类管理」维护，预置：生活用品/食品饮料/电子数码/图书教材/其他
- **商品排序**：智能排序，权重 = 发布时间 60% + 浏览量 30% + 临近下架剩余时间 10%；搜索结果按关键词匹配程度排序
- **举报机制**：商品详情页提供「举报」按钮，选举报理由 + 补充说明提交至后台；后台「社区管理」增加举报列表，管理员审核处理（下架/删除/忽略）；处理结果以站内信通知举报者和被举报者双方（通知模板不同）

#### 3.4.2 预留功能接口

- 预留社区模块扩展能力
- 每个功能支持后台独立启停，关闭时前端不显示，数据保留

#### 3.4.3 快讯

发布校内即时信息，支持按身份定向推送。浏览快讯需登录，无认证要求。

**发布类型与展示规则：**

| 发布类型 | 展示范围 | 说明 |
|:--------|:---------|:-----|
| 在校生 | 本科生 + 研究生 | 所有在校生可见 |
| 新生 | 仅新生 | 新生身份可见 |
| 本科生 | 仅本科生 | 本科生身份可见 |
| 研究生 | 仅研究生 | 研究生身份可见 |

**前端展示形式：**

- 列表页：时间线样式（发布时间 + 标题 + 摘要 + 类型标签）
- 详情页：富文本内容 + 发布时间 + 来源
- 顶部按类型筛选 Tab（全部/在校生/新生/本科生/研究生）
- 支持置顶（后台配置）

**后台配置内容：**

- 快讯 CRUD：标题、内容（富文本）、发布类型、置顶、排序、启停
- 支持将已发布的快讯转为「首页弹窗公告」
- 状态：草稿/已发布/已下架/弹窗公告
- 归入管理后台「社区模块」二级分类下，新增「快讯管理」三级分类

**首页弹窗公告规则：**

- 支持将「已发布」状态快讯设为首页弹窗公告，展示到对应服务身份首页。
- 弹窗公告按展示服务身份区分：

| 弹窗服务 | 展示范围 |
|:--------|:---------|
| 新生版 | 仅新生身份首页展示 |
| 在校生版 | 仅本科生 + 研究生（在校生）首页展示 |

- 同一时间内，新生版和在校生版各只允许存在 1 个启用中的首页弹窗公告。
- 已开启的弹窗公告支持手动关闭/下架；关闭后对应身份首页不再展示。
- 用户查看或关闭弹窗后，本次登录内不再重复弹出。

#### 3.4.4 校友圈

简易版论坛，分为**帖子**和**表白墙**两种类型，校友圈主页顶部两个 Tab 切换。

**类型一：帖子**

| 项 | 说明 |
|:--|:-----|
| 发帖 | 需选择版块分类后发布，不可匿名 |
| 内容 | 文字描述 |
| 前端列表 | 仅展示文字标题（+ 版块标签 + 发布者 + 时间） |
| 详情页 | 打开帖子后展示完整描述内容 |
| 互动 | 支持点赞、评论 |
| 排序 | 活跃时间倒序：活跃时间 = max(发布时间, 最后评论时间)；每有新帖子发布或新评论产生，对应帖子刷新到顶部；长期无评论的帖子自然沉到底部 |
| 置顶 | 管理后台可对帖子进行置顶操作 |

版块分类（后台可增删改，预置）：
- 海甸生活 / 儋州生活 / 城西生活 / 观澜湖生活
- 学习交流 / 考研交流 / 求职招聘
- 活动约伴 / 寻物启事

**类型二：表白墙**

| 项 | 说明 |
|:--|:-----|
| 发布 | 不需要选择版块分类，可匿名发布 |
| 内容 | 输入描述 + 可插入图片（最多 9 张，九宫格展示） |
| 匿名 | 匿名后不展示昵称/头像，仅展示「匿名用户」 |
| 前端展示 | 参考微信朋友圈样式（头像/昵称 + 文字 + 图片九宫格 + 时间 + 点赞/评论） |
| 互动 | 支持点赞、评论 |
| 排序 | 最新发布在前 |

**通用规则：**

- 发布帖子/表白墙需**已认证**（与二手集市一致），浏览需登录但无认证要求
- 评论支持**楼中楼**（可回复评论，形成嵌套层级）
- 支持**举报**（帖子/表白墙/评论均可举报）：
  - 举报理由：垃圾广告/人身攻击/色情低俗/虚假信息/其他（必选）+ 补充说明（选填）
  - 后台统一处理：下架/删除/忽略 + 处理备注
  - **处理结果通知**：管理员处理后以站内信通知举报者和被举报者双方（通知模板不同）
- 校友圈作为社区模块功能之一，支持后台独立启停
- 归入管理后台「社区模块」二级分类下，新增「校友圈管理」三级分类（帖子管理 + 表白墙管理 + 版块管理）

### 3.5 课表模块

#### 3.5.1 数据来源

- 手动DIY：用户逐条添加课程，仅课程名称为必填
- 文件导入：导入模板由运营方提供（格式待定）

#### 3.5.2 手动添加课程字段

| 字段 | 必填/选填 | 说明 |
|:----|:---------|:------|
| 课程名称 | 必填 | — |
| 授课教师 | 选填 | — |
| 上课地点 | 选填 | 如：3教-201 |
| 上课周次 | 选填 | 如：第1-16周 |
| 上课星期 | 选填 | 周一 ~ 周日 |
| 上课节次 | 选填 | 第几节到第几节 |
| 课程颜色 | 选填 | 预设15种颜色，不选则随机分配 |

**课程冲突处理：**

- 手动添加课程时，系统检测同一时段是否已有课程
- 检测到冲突：弹提示「与已有课程 XX 时间冲突，是否继续添加？」
- 用户选择继续：**直接覆盖冲突位置的原课程**（原课程被替换删除）
- 用户选择取消：不添加，返回编辑

#### 3.5.3 展示视图

- 周视图：默认视图，横轴周一到周日，纵轴节次
- 今日视图：只看当天课程，左右滑动切换日期
- 视图切换后长期记忆，下次打开沿用上次选择
- 课表数据按身份隔离

#### 3.5.4 课表分享

用户通过手动DIY或文件导入生成课表后，可分享课表给相同身份的其他用户。

生成分享：
- 用户点击「分享课表」，系统生成唯一分享码
- 分享码与分享者的课表数据绑定，同时绑定分享者的身份（本科生/研究生）

复刻课表：
- 其他用户在课表模块输入分享码，点击「复刻」
- 系统校验分享码对应的身份与当前用户身份是否一致
- 身份一致：将该分享码绑定的课程数据复制到当前用户课表
- 身份不一致：提示「分享码与你的身份不匹配」，拒绝复刻

规则：
- 分享码仅限相同身份之间生效：本科生分享码仅本科生账号可复刻，研究生分享码仅研究生账号可复刻
- 复刻为整表复制：将分享者的全部有效课程复制到当前用户课表，不覆盖当前用户已有课程
- **快照模式**：生成分享码时拍快照，分享者后续修改课表不影响已生成的快照
- **有效期**：分享码有效期 15 天，过期后不可复刻
- **数量上限**：每个用户最多同时有 3 个生效的分享码
- 分享码由后台「课表模块-分享码管理」统一管理，可查看/停用

### 3.6 工具箱

面向全体已登录用户（游客不可访问），提供实用工具增加使用频率。测试类工具题库来源为外部引用（稍后提供）。工具箱入口位于小程序首页宫格导航，不占用底部 Tab 位置。工具按分类管理，每个工具支持独立启停、积分消耗开关与积分数配置。

#### 3.6.1 工具分类

| 分类 | 工具数 | 说明 |
|:----|:------:|:-----|
| 生活娱乐 | 3 | 证件照生成、影视解析、摇骰子 |
| 学习工具 | 1 | 科学计算器 |
| 趣味测试 | 6 | SBTI/转盘/舒尔特/MBTI/黑暗三角/七宗罪VS七美德 |

分类由后台 CRUD 管理，可增删改。

#### 3.6.2 工具清单

**生活娱乐：**

| 工具 | 说明 | 分享 | 积分 |
|:----|:-----|:----:|:----:|
| 证件照生成 | 上传照片裁剪为常用尺寸（一寸/二寸/小二寸） | — | 免费 |
| 影视解析 | 输入视频链接解析播放；后台配置解析线路和 API 接口，支持多线路，不提供内容存储 | — | 免费 |
| 摇骰子 | 随机摇骰子，支持自定义骰子数量 | — | 免费 |

**学习工具：**

| 工具 | 说明 | 分享 | 积分 |
|:----|:-----|:----:|:----:|
| 科学计算器 | 科学函数计算（三角/对数/幂运算等） | — | 免费 |

**趣味测试：**

| 工具 | 说明 | 分享 | 积分 |
|:----|:-----|:----:|:----:|
| SBTI 人格测试 | 人格类型测试 | 链接+海报 | 免费 |
| 转盘模拟器 | 自定义选项转盘随机抽取 | 链接+海报 | 免费 |
| 舒尔特方格训练 | 注意力训练，5x5~10x10 多难度，记录完成用时 | 链接+海报 | 免费 |
| MBTI 人格测试 | 16 型人格测试 | 链接+海报 | 免费 |
| 黑暗三角人格测试 | 马基雅维利/精神病态/自恋测试 | 链接+海报 | 免费 |
| 七宗罪VS七美德测试 | 性格倾向测试 | 链接+海报 | 免费 |

#### 3.6.3 积分扣除机制

- 当前阶段所有工具积分消耗为 0（免费）
- 后台可对每个工具独立配置：
  - 是否开启积分消耗（points_enabled 开关）
  - 积分消耗数（points_cost）
  - 积分消耗模式（points_mode）：

| 模式 | 说明 |
|:----|:-----|
| 每次使用消耗（per_use） | 每次使用该工具都扣除配置的积分数 |
| 一次性消耗（one_time） | 初次使用时扣除一次积分，之后永久免费使用该工具，不限次数 |

**使用流程：**

- points_enabled = false → 免费，直接使用
- points_enabled = true + per_use → 每次检查积分是否充足，充足则扣除后使用，不足则拦截
- points_enabled = true + one_time → 查询是否已解锁（tool_unlocks 表有记录），已解锁直接使用；未解锁则检查积分是否充足，充足则扣除并写入解锁记录后使用，不足则拦截

**模式切换规则：** 切换消耗模式仅影响后续使用：
- 从「一次性」切到「每次」：已解锁用户也需重新每次付费
- 从「每次」切到「一次性」：之前每次付费不视为解锁，下次使用触发一次性扣费

#### 3.6.4 分享机制

- **链接分享**：生成结果链接，他人打开后展示测试结果
- **海报分享**：生成结果海报图片，附带二维码可直接扫码跳转该工具；海报模板样式统一，后台可配置
- **舒尔特方格**：不同难度（5x5~10x10）的分享海报/小程序主图需有区分（标注难度等级）

#### 3.6.5 权限

| 场景 | 游客 | 新生 | 本科生 | 研究生 |
|:----|:----:|:----:|:-----:|:-----:|
| 工具箱入口 | ❌（提示登录） | ✅ | ✅ | ✅ |
| 免费工具 | ❌ | ✅ | ✅ | ✅ |
| 积分消耗工具 | ❌ | ✅（需认证+积分充足） | ✅（需认证+积分充足） | ✅（需认证+积分充足） |

#### 3.6.6 测试要求

积分机制需纳入项目测试，测试用例覆盖：

| # | 测试场景 |
|:-:|:---------|
| 1 | 每次消耗模式：每次使用扣除积分 |
| 2 | 每次消耗模式：积分不足拦截 |
| 3 | 一次性消耗模式：首次使用扣除积分 |
| 4 | 一次性消耗模式：已解锁后再次使用不扣积分 |
| 5 | 一次性消耗模式：未解锁且积分不足拦截 |
| 6 | 积分开关关闭：工具免费使用 |
| 7 | 积分数变更后生效 |
| 8 | 免费工具不受积分影响 |
| 9 | 模式切换：一次性->每次，已解锁用户需重新付费 |
| 10 | 模式切换：每次->一次性，之前付费不累计，下次触发一次性扣费 |
| 11 | 分享功能（链接/海报生成/二维码跳转） |
| 12 | 舒尔特方格多难度海报区分 |

---

## 4. 开学/假期管理

### 4.1 模式切换（后台手动）

| 模式 | 行为 |
|:----|:------|
| 开学模式 | 小程序启动后直接进入主页 |
| 假期模式 | 显示前置导航页，需选择寒假/暑假并填写开学时间 |

### 4.2 前置导航页逻辑

- 未注册新用户初次访问：
  - 显示 [进入海大工坊] 按钮 → 身份选择页 → 微信授权 → 注入身份 → 注册成功 → 进入主页
  - 暑假模式下额外显示 [新生入口] 按钮（见 §4.3）
- 已注册老用户：显示 [进入海大工坊] 按钮 → 直接进入主页

### 4.3 暑假新生入口

- 暑假模式下，启动页（前置导航页）显示 [新生入口] 按钮
- **出现条件**：仅限暑假模式下访问的**未注册新用户**；已注册老用户访问时不显示该按钮
- **点击流程**：
  1. 新用户点击 [新生入口]
  2. 自动记忆「新生」身份
  3. 进入微信授权登录
  4. 授权成功后自动注入新生身份
  5. 注册成功（生成 UID + 默认密码）
  6. 直接进入新生版块页面
- 通过新生入口注册的用户，身份固定为「新生」，无需再走身份选择页

---

## 5. 通用设计原则

1. **内容型页面**（入学指南/生活攻略/海大介绍/乘车指南）统一使用富文本渲染，后台用富文本编辑器维护，支持图片、段落、列表
2. **列表型页面**（电话簿/找室友/时刻表/车站信息）统一卡片式设计 + 下拉刷新
3. **所有内容后台均支持**：排序、启停（停用后前端隐藏但数据保留）
4. **图片资源**：富文本内图片与独立图片字段均通过统一上传接口存储，单文件最大 5MB

---

## 6. 术语表

| 术语 | 说明 |
|:----|:------|
| 新用户 | 首次打开小程序，尚未选择身份的用户 |
| 老用户 | 已完成身份选择的用户 |
| 身份选择 | 新用户首次访问时在新生/本科生/研究生中选择 |
| 身份修改 | 在个人设置中修改身份，总计限3次 |
| 启停控制 | 后台对各功能的独立开关，关闭时前端隐藏但数据保留 |
| 分享码 | 课表分享生成的唯一代码，用于复刻他人课表，仅同身份生效 |
| 复刻 | 通过分享码将他人课表复制到自己的课表 |
| 富文本 | 支持图文混排的内容格式（段落/标题/图片/列表） |
| UID | 注册时自动生成的身份码，随机生成 6 位数字，全局唯一，生成时去重 |
| 认证状态 | 未认证 / 待审核 / 已认证，发布商品需已认证 |
| 隐私设置 | 用户控制其他用户可见的对外展示信息 |
| 在线用户 | 最近 30 分钟内有 API 请求的用户 |
| 快照 | 生成分享码时拍下的课表数据副本，不受后续修改影响 |
| 工具解锁 | 一次性消耗模式下用户付费解锁工具，记录于 tool_unlocks 表 |
